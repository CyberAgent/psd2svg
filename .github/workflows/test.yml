name: Test

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test-linux:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v7

      - name: Install fonts
        run: |
          sudo apt-get update
          echo ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true | sudo debconf-set-selections
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y \
            ttf-mscorefonts-installer \
            fontconfig \
            fonts-noto-core \
            fonts-noto-cjk \
            fonts-noto-ui-core
          sudo fc-cache -fv
          # The mscorefonts postinst downloads and extracts through an
          # update-notifier hook that can fail while apt still exits 0,
          # leaving the suite to render Arial and Times New Roman text in a
          # fallback face. Retry once, then assert.
          if ! fc-list | grep -q "Times New Roman"; then
            sudo DEBIAN_FRONTEND=noninteractive dpkg-reconfigure ttf-mscorefonts-installer
            sudo fc-cache -fv
          fi
          fc-list | grep -q "Times New Roman"
          fc-list | grep -q "Arial"

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --group dev -p ${{ matrix.python-version }}

      - name: Run linting
        run: uv run ruff check src/ tests/

      - name: Run formatting check
        run: uv run ruff format --check src/ tests/

      - name: Run type checking
        run: uv run mypy src/ tests/

      - name: Run tests
        run: uv run pytest

  test-windows:
    runs-on: windows-latest
    strategy:
      fail-fast: false
      matrix:
        # Reduced matrix: only test min and max supported versions
        python-version: ["3.10", "3.13"]

    steps:
      - uses: actions/checkout@v7

      - name: Install uv
        uses: astral-sh/setup-uv@v7
        with:
          enable-cache: true

      - name: Install dependencies
        run: uv sync --group dev -p ${{ matrix.python-version }}

      # Skip linting, formatting, and type checking on Windows
      # These are platform-independent and already verified on Linux

      - name: Run tests
        run: uv run pytest

      - name: Run Windows-specific font resolution tests
        run: uv run pytest tests/test_windows_fonts.py::TestWindowsRegistryIntegration -v
